let popUpBox = document.getElementById('popUp');

// 

function displayPopup() {
    popUpBox.classList.add("displaypop");
};

// 

function closePopup(params) {
    popUpBox.classList.remove("displaypop");
}